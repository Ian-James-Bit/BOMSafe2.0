directions:
    run (install): python -m venv virtual_environment
    run (activate): source virtual_environment/bin/activate
    run (install): pip install pandas openpyxl
    run (install): python -m pip install pytest
    run (install): pip install requests
    run (update): pip install --upgrade pip
    run (install): pip install python-dotenv
To run tests:
    python -m pytest
To run script/use:
    Upload BOM Excel sheet into input folder
    run (command): python -m src.main
    Download updated Excel sheet from output folder

